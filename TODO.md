# TODO list

1. Activity tab does not update based on selected date range
2. Changing the custom date does not refresh chart data, I have to switch tabs
3. If there is no data for that range, hide the charts
4. Limit commit distribution bar min width, bars look really tiny with lots of contributors. Better to expand chart height and limit bar width to like 10px
5. Remove code frequency tab and just have it on Overview tab below the `Top Contributors` chart
6. Instead of showing `Present` show the current date
7. If a user selects a date before the oldest commit, default to the oldest commit date
8. If a user selects a date after today's date, default to today's date
