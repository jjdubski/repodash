# TODO list

1. Add dropdown of right side of `Contribution Graph` on overview tab to filter contribution graph by author, commits, or lines added/deleted
2. Add a `Custom` date range filter that will show a calender picker, and allow user to select start and end date
3. Update total commits, contributors, lines added, lines deleted based on selected date range
4. Add dropdown on right side of `Top Contributors` on overview tab to show graph by commits (default), or lines added/deleted
5. Overall contributors should be listed by their github username, if no github username is found, then list by their email address (if email address is the same for multiple users, then just show one of them (group to username if possible))
6. Contributors page stats should update based on selected date range
